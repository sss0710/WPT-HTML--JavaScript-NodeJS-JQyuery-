module.exports.calcArea = function(len, bre)
{
    var area = len*bre;
    console.log(area);
}

module.exports.calPerim = function(len, bre)
{
    var perim = 2*(len + bre);
    console.log(perim);
}

